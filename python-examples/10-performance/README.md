# 10. Performance - 성능 최적화

> 💡 **핵심:**
> Python은 느릴 수 있지만, 올바른 패턴을 사용하면 충분히 빠릅니다.
> 프로파일링으로 병목을 찾고, 적절한 최적화를 적용하세요.

## 성능 최적화 원칙

```
┌─────────────────────────────────────────────────────────┐
│  Python 성능 최적화 순서                                 │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  1. 먼저 측정하라 (Profile first!)                      │
│  2. 알고리즘/자료구조 최적화                            │
│  3. 내장 함수 활용 (C로 구현됨)                         │
│  4. Comprehension > for loop                            │
│  5. 필요시 Cython, NumPy 활용                           │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## 📚 현재 상태

이 섹션은 로드맵 README만 있습니다. 실행 가능한 성능 예제는 아직 추가 전입니다.

## 🧭 확장 예정 주제

- cProfile 사용법
- 메모리 효율
- dict 최적화
- 문자열 연결 최적화

## 🚀 실행 방법

```bash
# 프로파일러 설치 (선택)
pip install line-profiler memory-profiler

# 예제 추가 후 프로파일링 실행
python -m cProfile -s tottime <example_file>.py
```
